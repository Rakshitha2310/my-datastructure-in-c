//Read operation using array
#include<stdio.h>
int main(){
    int arr[]={10,20,30,40,50};
    int n=sizeof(arr)/sizeof(arr[0]);
    int index = 3;
    if(index>=0 && index<n){
        printf("%d ",arr[index]);
    }
}
